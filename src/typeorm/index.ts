import { Users } from '../user/entities/user.entity';
import { Platforms } from '../platform/entities/platform.entity';

const entities = [Users, Platforms];

export { Users, Platforms };

export default entities;
